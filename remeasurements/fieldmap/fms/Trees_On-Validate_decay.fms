begin
  
  if (Trees['decay'] <> NULL) and (Trees_old['decay_old'] <> NULL) then begin
  
    if Trees['decay'] < Trees_old['decay_old'] then begin
      showwarning('Decay is lower than the previous census. Please double-check.');
    end;
    
  end;

end.